/**
 * useAuth hook — re-exported from auth-provider for ergonomic import.
 *
 * Usage:
 *   import { useAuth } from '@/hooks/use-auth'
 *   const { user, token, logout } = useAuth()
 */
export { useAuth } from '@/providers/auth-provider'
