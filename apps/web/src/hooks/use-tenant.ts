/**
 * useTenant hook — re-exported from tenant-provider for ergonomic import.
 *
 * Usage:
 *   import { useTenant } from '@/hooks/use-tenant'
 *   const { tenant, role, isLoading } = useTenant()
 */
export { useTenant } from '@/providers/tenant-provider'
